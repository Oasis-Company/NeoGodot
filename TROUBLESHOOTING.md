# NeoGodot 故障排除指南

本指南将帮助你解决在使用 NeoGodot 过程中可能遇到的常见问题。

## 目录

1. [插件相关问题](#插件相关问题)
2. [Runtime Gateway 相关问题](#runtime-gateway-相关问题)
3. [AI 生成相关问题](#ai-生成相关问题)
4. [编译和运行问题](#编译和运行问题)
5. [其他问题](#其他问题)

---

## 插件相关问题

### 插件无法启用

**问题描述**：在 Godot 编辑器的项目设置中启用 NeoGodot 插件时失败。

**可能原因和解决方案**：
1. **Godot 版本不匹配**
   - 确认使用 Godot 4.x 版本
   - 下载并安装最新的 Godot 4.x 编辑器

2. **插件目录结构不完整**
   - 检查 `addons/neo_godot/` 目录是否包含所有必要文件
   - 重新复制插件目录到项目中

3. **插件配置错误**
   - 查看编辑器控制台的错误信息
   - 检查 `plugin.cfg` 文件是否存在且格式正确
   - 尝试重新加载项目（项目 → 重新加载当前项目）

4. **插件与其他插件冲突**
   - 暂时禁用其他插件，然后单独启用 NeoGodot
   - 如果问题解决，逐个启用其他插件以定位冲突

### 插件启用后没有显示 AI 助手面板

**问题描述**：插件已启用，但编辑器右侧没有出现「NeoGodot AI 助手」面板。

**解决方案**：
1. 检查编辑器底部的「Dock」区域是否有该面板标签
2. 尝试重新加载项目
3. 查看编辑器控制台是否有相关错误信息
4. 确保插件的 UI 场景文件（`.tscn`）完整

### 配置按钮没有响应

**问题描述**：点击工具栏的「NeoGodot 配置」按钮没有反应。

**解决方案**：
1. 确保插件已正确启用
2. 查看编辑器控制台是否有错误
3. 尝试通过「项目 → 项目设置 → 插件」禁用并重新启用插件

---

## Runtime Gateway 相关问题

### Runtime Gateway 启动失败

**问题描述**：运行 `python main.py` 或启动脚本时失败。

**诊断步骤**：
1. 查看控制台输出的错误信息
2. 检查 Python 版本：`python --version`（需要 3.9+）
3. 确认依赖已安装：`pip install -r requirements.txt`

**常见解决方案**：

**错误：ModuleNotFoundError**
```
pip install -r requirements.txt
```

**错误：Address already in use（端口被占用）**
- 方法 1：修改 `.env` 文件中的 `PORT` 为其他端口
- 方法 2：关闭占用端口的程序
  - Windows：`netstat -ano | findstr :8000` 查找 PID，然后在任务管理器中结束进程
  - Linux/Mac：`lsof -ti:8000 | xargs kill -9`

**错误：.env file not found**
```bash
cd runtime
cp .env.example .env
# 编辑 .env 文件配置必要参数
```

### 无法连接到 Runtime Gateway

**问题描述**：Godot 插件提示无法连接到 Runtime Gateway。

**诊断和解决方案**：
1. **确认 Runtime Gateway 正在运行**
   - 检查 Runtime Gateway 控制台是否有启动成功的日志
   - 访问 http://localhost:8000/v1/health 验证（应返回 {"status": "healthy"}）

2. **检查 URL 配置**
   - 确认 Godot 插件配置中的 Runtime Gateway URL 正确
   - 默认应为 `http://localhost:8000`

3. **检查防火墙设置**
   - 暂时禁用防火墙测试
   - 确保允许 localhost 连接

4. **检查端口配置**
   - 确认 Runtime Gateway 的监听端口与插件配置一致
   - 查看 `.env` 文件中的 `PORT` 配置

### Runtime Gateway 返回 500 错误

**问题描述**：API 请求返回 500 Internal Server Error。

**解决方案**：
1. 查看 Runtime Gateway 控制台的详细错误日志
2. 检查 `.env` 文件配置是否正确
3. 确认所有依赖包已正确安装
4. 尝试启用调试模式（设置 `DEBUG=true`）获取更详细的错误信息

---

## AI 生成相关问题

### AI 生成没有响应

**问题描述**：点击「生成」按钮后长时间没有响应。

**可能原因**：
1. **API Key 未配置或无效**
   - 检查 `.env` 文件中的 API Key 配置
   - 确认 API Key 有效且有额度

2. **网络连接问题**
   - 检查网络连接
   - 确认可以访问 AI 提供商的 API 端点

3. **请求超时**
   - 查看 Runtime Gateway 日志，确认请求是否到达
   - 检查超时设置

**解决方案**：
1. 首先验证健康检查端点是否正常
2. 查看编辑器控制台和 Runtime Gateway 控制台的错误信息
3. 尝试使用简单的提示词测试
4. 检查 API 提供商的服务状态

### 生成的内容质量不佳

**问题描述**：AI 生成的代码或场景不符合预期。

**改进建议**：
1. **优化提示词**
   - 提供更详细、更具体的需求描述
   - 明确指定所需的功能和行为

2. **使用上下文**
   - 在提示词中提供相关的代码片段或场景信息
   - 说明项目的技术栈和风格要求

3. **多次尝试**
   - AI 生成具有随机性，多次尝试可能获得更好的结果
   - 可以在原结果基础上进行迭代优化

4. **手动调整**
   - 生成的内容通常需要一些手动调整和优化
   - 将 AI 生成作为起点，而非最终产品

### 撤销/重做不工作

**问题描述**：AI 生成操作后无法撤销。

**解决方案**：
1. 确认使用的是标准的编辑器撤销功能（Ctrl+Z / Cmd+Z）
2. 检查操作是否在撤销历史记录内
3. 某些操作可能不支持撤销，查看控制台是否有相关提示

---

## 编译和运行问题

### Godot 引擎编译失败

**问题描述**：从源码编译 NeoGodot 引擎时失败。

**解决方案**：
1. 确认已安装所有编译依赖
   - Windows：Visual Studio 2022（C++ 桌面开发工作负载）
   - macOS：Xcode 命令行工具
   - Linux：参考 QuickStart.md 中的依赖列表

2. 检查 SCons 命令参数
   ```bash
   scons platform=windows  # Windows
   scons platform=macos    # macOS
   scons platform=linuxbsd # Linux
   ```

3. 查看编译错误信息，根据提示解决具体问题
4. 尝试清理构建后重新编译
   ```bash
   scons platform=windows --clean
   scons platform=windows
   ```

### 示例项目无法运行

**问题描述**：打开 `ai_generated/` 目录下的示例项目时出错。

**解决方案**：
1. 确认使用 Godot 4.x 打开项目
2. 检查项目设置中的资源路径是否正确
3. 查看编辑器控制台的错误信息
4. 尝试重新导入项目资源
5. 确认所有依赖节点和脚本都存在

### 游戏导出失败

**问题描述**：导出游戏时出现错误。

**解决方案**：
1. 确认已安装对应平台的导出模板
2. 检查导出预设配置是否正确
3. 查看导出日志中的错误信息
4. 确保项目中没有损坏的资源
5. 尝试清理导出缓存后重新导出

---

## 其他问题

### 性能问题

**症状**：编辑器或游戏运行缓慢。

**优化建议**：
1. 使用编辑器的 Profiler 分析性能瓶颈
2. 减少场景中的节点数量
3. 优化纹理大小和压缩设置
4. 启用 occlusion culling（3D）
5. 使用 LOD（Level of Detail）
6. 减少 draw calls

### 资源丢失

**问题描述**：项目中的资源显示为丢失状态。

**解决方案**：
1. 检查资源文件是否存在于正确路径
2. 使用「项目 → 重新导入」重新导入资源
3. 检查资源的 `.import` 文件是否存在且正确
4. 确保资源文件名和路径中没有特殊字符

### 获取更多帮助

如果以上方案无法解决你的问题：
1. 查看 [README.md](README.md) 和 [QuickStart.md](QuickStart.md)
2. 搜索项目的 GitHub Issues
3. 提交新的 Issue，包含：
   - 详细的问题描述
   - 复现步骤
   - 错误日志或截图
   - 你的系统环境信息（操作系统、Godot 版本、Python 版本等）

---

**提示**：遇到问题时，首先查看相关的控制台日志（编辑器控制台或 Runtime Gateway 控制台），错误信息通常会提供解决问题的线索。
